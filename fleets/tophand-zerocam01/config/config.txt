# Pi Zero 2 W + IMX708 + BG95-M3 Configuration
# Matches working config from tophand-zero-04
# Apply via balena config vars: BALENA_HOST_CONFIG_<key>

# Enable I2C (required for camera)
dtparam=i2c_arm=on

# Audio (optional)
dtparam=audio=on

# Camera auto-detect (works on pi-zero-04 with IMX708)
camera_auto_detect=1

# Display auto-detect
display_auto_detect=1

# Standard Pi settings
auto_initramfs=1
dtoverlay=vc4-kms-v3d
max_framebuffers=2
disable_fw_kms_setup=1
arm_64bit=1
disable_overscan=1
arm_boost=1

[cm4]
otg_mode=1

[cm5]
dtoverlay=dwc2,dr_mode=host

[all]
enable_uart=1

# IMX708 Camera Module 3 overlay
dtoverlay=imx708
